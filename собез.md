5 причин почему происходит rerender

### Разлдичие Prototype Class

Классы не поднимаются
Тело класса всегда используется в strict режиме
static - метод свойство самого класса Number.MAX_SAFE_INTEGER, Array.from()
